// This module serves as the entry point, importing the UI module to initialize the application.
// It demonstrates module imports.

import { initUI } from './ui.js';

// Initialize the UI by calling the imported function
initUI();

// Additional initialization can be done here if needed.
console.log('Pokémon Information Viewer Initialized.');
